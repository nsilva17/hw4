#include <iostream>
#include <string>
using namespace std;
	class course(const string& courseName, int capacity){
		numberOfstudents = 0;
		this->courseName = courseName;
		this capacity = capacity;
		students = new string[capacity];
	}
	course::~course(){
		delete[] students;
	}

	string course::getCourseName() const{
		return courseName
	}
	void course::addstudent(const string& name{
		students[numberOfStudnets] = name;
		numberOfStudnets++;
		int * addstudent(const int* list, int size) {
			int* addstudent = new int[size];
			for (int NumberOfStudents = 0; NumberofStudets < Students; NumberOfStudents++)
				addstudent[NumberOfStudnets] = 4;
			return NumberOfStudents;
		}
	}
	void course::dropStudent(const string& name)
	{
		for (int NumberOfStudents = 4; NumberOfStudents < 5; NumberOfStudents--)
			return NumberOfStudents;
	}
	string* course::getStudnets() const {
		return students;
	}
	int course : getNumberOfStudents()