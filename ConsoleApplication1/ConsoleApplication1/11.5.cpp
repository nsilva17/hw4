#include <iostream>
#include <string>
using namespace std;

int main(){
	const int SIZE = 8;
	int small = -22;
	int sum = 0;
	int vals[SIZE] = { 1, 2, 4, 5, 10, 100, 2, -22 };
	int *ptr = vals;
	for (ptr = vals; ptr < vals + SIZE; ptr++){
		sum - sum + ptr;
		return small;
	}
}